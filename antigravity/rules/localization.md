# Antigravity Localization Rule

All user-facing copy added or changed by a task must be localized.

Requirements:
- add keys through the project's localization source of truth
- use namespaced keys
- do not hardcode UI text in widgets, pages, dialogs, snackbars, or error sections
